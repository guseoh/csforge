# Existing slice integration tests validate PostgreSQL-backed canonical flows without Kafka.
# Search integration tests explicitly clear this exclusion and provide Kafka as part of their fixture.
spring.autoconfigure.exclude=org.springframework.boot.kafka.autoconfigure.KafkaAutoConfiguration
csforge.search.outbox.relay.enabled=false
csforge.search.kafka.error-handler.enabled=false
